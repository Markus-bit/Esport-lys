import { DiscoveryBridgeDefinition } from './discoveryTypes';
export declare class SSDPSearch {
    private responseEmitter;
    private socket;
    constructor();
    search(timeout?: number): Promise<DiscoveryBridgeDefinition[]>;
    private _start;
    private finished;
}
