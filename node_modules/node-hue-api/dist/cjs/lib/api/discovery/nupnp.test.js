"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const chai_1 = require("chai");
const nupnp_1 = require("./nupnp");
describe('N-UPnP', () => {
    it('should discover a bridge on the network', async () => {
        const results = await nupnp_1.nupnp();
        chai_1.expect(results).to.be.instanceOf(Array);
        chai_1.expect(results[0]).to.have.property('id');
        chai_1.expect(results[0]).to.have.property('internalipaddress');
    });
});
