import { Api } from '../Api';
export declare class LocalInsecureBootstrap {
    readonly baseUrl: string;
    readonly hostname: string;
    constructor(hostname: string, port?: number);
    connect(username: string, clientkey?: string): Promise<Api>;
}
